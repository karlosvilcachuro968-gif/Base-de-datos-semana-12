begin transaction : inicia la transacción.

commit transaction : confirma los cambios si no hay errores.

rollback transaction : revierte todos los cambios si ocurre un error.

Try  catch : estructura para manejar errores en sql server.

throw : fuerza un error personalizado (por ejemplo, si el saldo queda negativo).





